{
  "name": "polyglot-repository-discovery",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck:discovery": "tsc --project tsconfig.json --noEmit",
    "test:discovery": "vitest run tests"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}

